name: daily-cron
on:
  schedule:
    - cron: "1 */3 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl -X GET https://veggies-fruits.vercel.app/api/databasecheck
